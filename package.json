{
  "name": "jenkins-conservatory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run build && node test/run.mjs",
    "test:only": "node test/run.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^7.0.0"
  }
}
